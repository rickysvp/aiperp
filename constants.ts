export const INITIAL_BALANCE = 0; // Initial USDC balance (need to swap from MON)
export const INITIAL_MON_BALANCE = 10000; // Initial MON balance for gas and swapping (10,000 MON)
export const MINT_COST = 500; // MON cost to mint an agent
export const SWAP_RATE = 0.02; // 1 MON = 0.02 USDC (1 USDC = 50 MON)
export const MAX_AGENTS = 2000; // Increased cap for massive battles
export const GAME_TICK_MS = 1000; // Battle happens every 1 second

// Visuals
export const MONAD_PURPLE = '#836EF9';
export const NEON_GREEN = '#00FF9D';
export const NEON_RED = '#FF0055';
